'use client';
import ImageUpload from '@/components/(app)/orders/[id]/image-reserve/image-upload';

const UploadImage = () => {
  return (
    <div className="mx-auto max-w-7xl sm:px-6 lg:px-8">
      <ImageUpload />
    </div>
  );
};

export default UploadImage;
