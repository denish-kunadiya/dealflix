'use client';
import ImageReserve from '@/components/(app)/orders/[id]/image-reserve/image-reserve';

const ImageReservePage = () => {
  return (
    <div className="mx-auto max-w-7xl sm:px-6 lg:px-8">
      <ImageReserve />
    </div>
  );
};

export default ImageReservePage;
